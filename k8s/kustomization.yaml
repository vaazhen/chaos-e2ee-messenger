apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: chaos-messenger

resources:
  - namespace.yaml
  - configmap.yaml
  - backend.yaml
  - frontend.yaml
  - ingress.yaml
  - autoscaling.yaml
  - disruption-budgets.yaml
