# Example only. Do not commit real values.
# In production, create this Secret through External Secrets Operator,
# Sealed Secrets, Vault, or the cloud provider's secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: chaos-messenger-secret
  namespace: chaos-messenger
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_WITH_A_STRONG_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_A_STRONG_PASSWORD"
  JWT_SECRET: "REPLACE_WITH_AT_LEAST_32_RANDOM_BYTES"
  VAPID_PRIVATE_KEY: ""
