services:
  # ── Caddy with self-signed HTTPS for WebCrypto (no domain needed) ──
  caddy:
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: 168.222.141.25

  # ── Grafana for dashboards (http://IP:3000) ──
  grafana:
    ports:
      - "3000:3000"
